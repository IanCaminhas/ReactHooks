//São as ações geradas sobre os números(atributo number)
//separar em arquivos assim, deixa o código mais legível para funções complexas
//Exemplo: interações com uma API(várias linhas)

export function numberAdd2(dispatch){
    dispatch({type: 'numberAdd2'})
}

